export const BODY = document.body;
export const MOBILE_TOGGLE = document.querySelector(
  ".mobile-toggle"
) as HTMLElement;
export const SCREEN_LEFT = document.querySelector(
  ".screen__left"
) as HTMLElement;
export const SCREEN_RIGHT = document.querySelector(
  ".screen__right"
) as HTMLElement;
export const SCREENS = document.querySelectorAll(
  ".screen"
) as unknown as HTMLElement;
